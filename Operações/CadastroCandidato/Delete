// Delete CANDIDATO
app.delete('/candidato/delete', function (req, res) {
    const { idCliente } = req.body;

    conn.query('DELETE FROM tbCandidato WHERE idCliente = ?',
        [idCliente], function (error, results) {
            if (error) throw error;
            res.status(200).json(results)
        })

});